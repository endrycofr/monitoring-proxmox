apiVersion: 1

datasources:
  - name: influxdb
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: true
    jsonData:
      version: Flux
      organization: "my-org"
      defaultBucket: "proxmox"
    secureJsonData:
      token: "my-super-secret-auth-token"
